#include "declare_cell.h"

void declare_cell_create_sub_object_views(ObjectView *declare_cell_view) {
//    auto declare_cell = (DeclareCell *) &declare_cell_view->object;
//
//    auto effect_view = new_object_view(declare_cell->);
//    auto next_view = new_object_view(do_then->next);
//
//    signal_listen(&declare_cell_view->position_sig, new function<void(void)>([=]() {
//        box_touch_left_right(&declare_cell_view->box, &effect_view->box);
//        box_align_up(&declare_cell_view->box, &effect_view->box);
//
//        box_align_left(&declare_cell_view->box, &next_view->box);
//        box_touch_up_down(&declare_cell_view->box, &next_view->box);
//
//        signal_update(&effect_view->position_sig);
//        signal_update(&next_view->position_sig);
//    }));
}